; Ultra-minimal XQuery outline test
; Just testing if any nodes are detected at all

(function_declaration
    local: (identifier) @name
    (param_list) @context
) @item

(variable_declaration) @item
(namespace_declaration) @item
